Note:
關於 peewee
被關聯的模型可以透過關聯模型呼叫, 比如: inventory 被 player 關聯

player = Player.get_or_create(user_id)
player.inventory <- 可以用這個寫法開始查詢

apple = player.inventory.get(Inventory.item_id == "apple")
print(f"{apple.quantity}") <- 回傳物品數量
print(f"{apple.corrupt_time}") <- 回傳腐壞時間 (未在此實作)

但這是因為 inventory 屬於一對多欄位，所以可以這樣呼叫循環 (專案裡的 status 也屬於此)
for item in player.inventory:
    print(f"{item.item_id}{item.quantity}")


Note:
關於指令

右鍵使用的應用程式指令語法糖為 @app_commands.context_menu(name="指令名稱")


Peewee 方法保留字，設計方法時不要使用以下名稱：
create()
save()
delete()
delete_instance()
select()
update()
insert()
get()
get_by_id()
get_or_create(**kwargs)
get_or_none(**kwargs)

目前專案有特別設計的單詞方法：
add() # status.py
remove() # status.py

#---
Peewee 有個物件取出的簡寫法，在已知主鍵 (Primary Key) 時，一般而言會寫：

player = Player.get(Player.id == player_id)

可以寫成：

player = Player.get_by_id(player_id)

cores
- 放底層邏輯
models
- 放資料庫物件
systems
- 放業務邏輯

Template Rendering 模板渲染
Data Binding 資料綁定
Edge Case Testing 邊界測試
Explicit Design 顯式設計
MVC（Model-View-Controller）架構
ECS（Entity-Component-System）架構
Decoupling 解耦
Separation of Concerns 關注點分離
中介編程 (Metaprogramming)
內省 (Introspection)
Response Schema (回應結構)

embed 格式

add_field   
set_author
set_image

{
    "author": {
        "name": name
        "icon_url": icon_url
    }
}


button = discord.ui.Button()

有幾個方向要解決:
宣告時要傳入什麼 label, style, emoji.. etc. 參數
最重要的 button.callback

構思:
- 如何在 message.yaml 裡標註 view 物件內容並取用？

解決方案:
- 獨立宣告 view 物件, 並使用 view_manager 於 bot.py 初始化時註冊
- 保存訊息格式的 message.yaml 標註要存取的 view 物件 id, 在 boardcast.create() 從 view_manager 取出

優勢:
- button 直接在 view 物件裡寫好, 不需要額外在 message.yaml 裡標註


構思:
- 如何統一輸出的 payload？

方案:
- 讓單元運算回傳結果再打包, 如 result = Player.add_experience(player_id, experience)


職責劃分:
action.execute 本身只計算執行這個動作的邏輯，不會包含結算與輸出訊息 (這兩個計算派出去給別的邏輯處理)